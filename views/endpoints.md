<!-- GENERATED by ai-control. Do not edit by hand; edit the model. -->

# Endpoints

## Module: Auth

### Login (`endpoint:auth.login@backend`)

- `POST /auth/login` · auth: public · status: applied
- Uses services: service:auth.auth-service@backend
- Touches entities: entity:user

## Module: Dashboards

### Get Widget Chart Data (`endpoint:dashboards.chart-data@backend`)

- `GET /dashboards/:id/widgets/:widgetId/data` · auth: authenticated · status: applied
- Uses services: service:dashboards.dashboards-service@backend
- Touches entities: entity:chart-widget

### Create Dashboard (`endpoint:dashboards.create@backend`)

- `POST /dashboards` · auth: authenticated · status: applied
- Uses services: service:dashboards.dashboards-service@backend
- Touches entities: entity:dashboard

## Module: Data

### List Data Files (`endpoint:data.list-files@backend`)

- `GET /data/files` · auth: authenticated · status: applied
- Uses services: service:data.data-service@backend
- Touches entities: entity:csv-file

### Update Column Descriptions (`endpoint:data.update-columns@backend`)

- `PATCH /data/files/:fileId/columns` · auth: authenticated · status: applied
- Uses services: service:data.data-service@backend
- Touches entities: entity:column-metadata

### Upload CSV File (`endpoint:data.upload-file@backend`)

- `POST /data/upload/file` · auth: authenticated · status: applied
- Uses services: service:data.data-service@backend
- Touches entities: entity:csv-file

