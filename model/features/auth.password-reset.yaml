id: feature:auth.password-reset
kind: Feature
name: Password Reset
description: Request a reset link by email and set a new password via a tokenized link.
status: applied
spec:
  moduleId: module:auth
  visibility: both
  subfeatures:
    - request reset link (emails a tokenized URL via MailJet)
    - validate token and set new password
