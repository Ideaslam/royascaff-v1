# Project Status

_Last updated: 2026-07-02 — initialized (blueprint not yet generated)_

> **Read this first when resuming work.** It is the bird's-eye view of what is built (`done`), in
> progress (`partial`), still to do (`planned`), and postponed (`deferred`). It is rolled up from the
> per-artifact status in `project/actions/**` and each `_index.md`. Status vocabulary and rules live in
> `engine/conventions.md` → **Build Status**. Template: `engine/templates/status-template.md`.

## Snapshot

| App | Services | Endpoints | Pages/Views | Overall |
|-----|----------|-----------|-------------|---------|
| _(not yet generated)_ | — | — | — | — |

> The `project/` blueprint (actions specs + `_index.md` registries) has not been generated yet. This
> workspace contains real code (`linda-api`, `linda-web`) — run **Phase R** (`engine/flows/reverse-engineer.md`)
> to scan the code and populate the blueprint. That flow sets each artifact's status from the code
> (`done`/`partial`) and fills in this dashboard at step **R.Done.1**.

## By Module

| Module | Services | Endpoints | Pages/Views | Status |
|--------|----------|-----------|-------------|--------|
| _(pending blueprint generation)_ | — | — | — | — |

## In Progress (`partial`)

- _(none recorded yet)_

## Next Up (roadmap, ordered)

1. Run `engine/flows/reverse-engineer.md` (Phase R) to generate the blueprint and populate this dashboard.

## Deferred (`deferred`)

| Artifact | App · Module | Reason | Revisit when |
|----------|--------------|--------|--------------|
| _(none)_ | | | |
