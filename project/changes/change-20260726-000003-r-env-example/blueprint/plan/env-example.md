# Env example (delta)

Create `roya-sales-ai-api-v2/.env.example` with placeholders for:

PORT, NODE_ENV, JWT_SECRET, JWT_ACCESS_EXPIRES_IN, JWT_REFRESH_EXPIRES_IN, APP_URL, ALLOWED_ORIGINS, MONGODB_URI, REDIS_URL, AWS_*, MAILJET_*, META_WHATSAPP_*, SETTINGS_ENCRYPTION_KEY, CREATIVE_SECTION_MAX_TOKENS, CREATIVE_SECTION_TEMPERATURE

No real secrets. Claude API key is workspace settings — note in comments.
