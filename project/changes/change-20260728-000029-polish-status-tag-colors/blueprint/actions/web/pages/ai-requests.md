# Pages — AI Requests (polish delta)

### AI Requests List `PG-AIREQ-01`

## Delta
- Human-facing label for status `retrying` is **`inprogress`** (table tag + filter option label).
- Filter `value` remains `retrying` so list API query is unchanged.
- Severity for `retrying` stays `warn`.
