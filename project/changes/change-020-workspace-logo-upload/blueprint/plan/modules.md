# Modules — delta (change-020)

## 9. Settings & Config

### Features (after-state for this pack)

1. **Workspace Settings** [both] — company/integration/financial/theme (schema-driven) **+ company logo upload/remove**
2. **Global Config Bundle** [both] — unchanged
3. **Maintenance Mode** [both] — unchanged
4. **Pipeline v3 feature flag** [both] — unchanged
5. **Workspace Logo** [both] — upload/delete to R2; preview on Settings Company tab; sidebar uses `logoUrl` with Safqa brand fallback

## Delta

- **Add** feature **Workspace Logo** under Settings & Config (mirrors Clients → Client Logo pattern)
- **Extend** Workspace Settings feature note to include company logo
